{
  "name": "agentic-deployment",
  "private": true,
  "packageManager": "pnpm@11.5.1",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "start:wd": "pnpm --filter @agentic-deployment/web-dashboard dev",
    "start:as": "pnpm --filter @agentic-deployment/api-server dev"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
